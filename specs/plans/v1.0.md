# Plan v1.0.0 — Stable Release

**Status:** in progress — Phases 1-4 done except the actual publish step (2026-08-06).
All work is committed on local branch `release/v1.0.0`; `pnpm release:check` is green at
`1.0.0`. Waiting on explicit user go-ahead to push/merge before `release.yml` auto-publishes.
**Entry state:** v0.9.0 published as RC; API frozen; `MIGRATION.md` drafted
**Goal:** ship `@voltui/components@1.0.0` + `@voltui/cli@1.0.0`. No new features — this
minor removes deprecations, runs final verification, and formalizes post-v1 policy.
**Branch:** `release/v1.0.0` (local, not pushed — publish only happens after explicit
user go-ahead to merge to `main`, since `release.yml` auto-publishes to npm on every
push there).

---

## Context primer (replaces repo analysis — trust this, verify cheaply)

- Everything is expected DONE at entry: forms contract (v0.5), overlay contract (v0.6),
  final component list (v0.7), theme audit + docs (v0.8), consumer validation + API
  freeze (v0.9). If any entry check below fails, STOP and finish the corresponding plan
  first — do not absorb its work here.
- Key files: `public-api.ts` (frozen surface, inventory in `specs/api-freeze-0.9.md`),
  `COMPONENT_STATUS.md`, `MIGRATION.md`, `CHANGELOG.md`, publish scripts `publish:lib` /
  `publish:cli` in root `package.json`.

**Verify entry state:**

```bash
node -p "require('./package.json').version"    # expect 0.9.0
test -f MIGRATION.md && echo ok                 # expect ok
grep -c experimental COMPONENT_STATUS.md        # expect 0 (or only post-v1 section)
pnpm release:check                              # expect green
```

---

## Phase 1 — Remove deprecations & final API check ✅ done (2026-08-06)

- [x] Removed all three deprecated aliases from 0.9.0's freeze: `VoltNavigationMenuLink`'s
      kebab-case `volt-navigation-menu-link` selector (+ its dev-mode warning),
      `VoltFileUpload`/`VoltFileDropzone`'s `dragOver` output, `VoltResizableHandle`'s
      `resizing` output. Updated/removed the matching dedicated deprecation tests in
      each component's spec. Regenerated `src/app/lib/api-reference.generated.ts`
      (`pnpm generate:api-reference` — not covered by `check:ai-docs`, has to be run by
      hand or the live docs API-reference panel goes stale).
- [x] Diffed current public API against `specs/api-freeze-0.9.md` (regenerated to a
      scratch copy via `node scripts/generate-api-freeze.mjs`, diffed, then restored the
      frozen file — never overwrote it). Confirmed the only differences from the 0.9.0
      snapshot are the three removed aliases (outputs count 42 → 39); no other drift.
- [x] `MIGRATION.md` was already accurate for this exact removal (drafted during 0.9.0
      specifically anticipating it) — no changes needed.

## Phase 2 — Full regression & final audits ✅ done (2026-08-06)

- [x] `pnpm test:all` green: 266/266 unit tests (56 files), 20 docs e2e, 2 npm-consumer
      e2e, 1 CLI-consumer e2e. (266, not 268 — two dedicated deprecation-alias tests
      were removed in Phase 1, not a regression.)
- [x] Consumer fixtures (`test:e2e:consumer`, `test:e2e:consumer-cli`) green against the
      final build.
- [x] Delegated to `a11y-reviewer` and `ssr-safety-reviewer` subagents for a pre-1.0
      sweep. Found and fixed real bugs (see below) — same pattern as the v0.9 Phase 1
      sweep that found the `VoltDropdownMenuItem`/`VoltInputOtp` bugs.
  - **SSR crash (ssr-safety-reviewer, confirmed empirically against the SSR dev
    server):** `VoltResizableHandle.ngAfterViewInit()` called `syncMeasurements()`
    unconditionally, which calls `getBoundingClientRect()` — that runs during SSR too
    (`ngAfterViewInit` is not browser-only), crashing every server render of
    `/docs/components/resizable` and guaranteeing a hydration mismatch. Fixed by
    wrapping the initial measurement in `afterNextRender()` (matches the existing
    `code-panel.ts` pattern). Re-verified against both the SSR dev server (error gone,
    HTTP 200) and a full production build + `wrangler pages dev` Lighthouse pass
    (CLS 0.00 — confirms no hydration-mismatch layout shift from the fix).
  - **Missing ARIA-contract test coverage (a11y-reviewer):** dialog/drawer unit specs
    never actually opened the overlay, so nothing asserted `role="dialog"`/
    `aria-modal`/`aria-labelledby`; the toast error variant's `role="alert"` had zero
    coverage anywhere. Attempted unit-level tests first, but both are structurally
    untestable in this jsdom + testing-library-angular setup: dialog/drawer's
    `NgpDialog` uses Angular CDK's `Overlay`/`DomPortalOutlet`, which throws
    (`Cannot read properties of undefined (reading 'createEmbeddedView')`) when opened
    via click in jsdom (unlike popover/dropdown-menu/tooltip, which use
    ng-primitives' own lightweight portal and open fine in unit tests); `VoltToast`
    can't render standalone either since `NgpToast` injects an `NgpToastOptions`
    context only `NgpToastManager` ever constructs. Added the assertions to
    `e2e/consumer.spec.ts` instead (real browser, real CDK overlay) — added a second
    error-variant toast trigger to `e2e/consumer/src/main.ts` since none existed.
  - **Informational, not fixed (out of this plan's scope):** `ssr-safety-reviewer` also
    found `window is not defined` inside `ng-primitives@0.110.2` itself (not this
    repo's code) when a listbox has an initial active/selected option during SSR
    (`ng-primitives-internal.mjs` → `getScrollableAncestor`). Needs an upstream
    ng-primitives fix or version bump — flagging for awareness, not fixing here (a
    dependency bump this late in the release needs its own verification pass, out of
    scope for this plan). Also a lower-severity note: `dropdown-menu-trigger.ts`'s
    keyboard-open-first-item logic queries `[role="menuitem"]` against the whole
    `ownerDocument` rather than scoping to its own menu content, so it could pick up
    another open dropdown's items if a consumer renders two at once — not fixed, minor.
- [x] Accessibility smoke pass in a real browser (`pnpm dev` + `playwright` MCP,
      per `CLAUDE.md`'s protocol): dialog — opens on click, focus moves inside
      automatically (to the first field), `role="dialog"`, Escape closes, focus returns
      to the trigger button — all confirmed via accessibility snapshot, not just
      assertions. Verified the `v1.0.0` header badge and accurate stable/beta labels
      (no experimental) render correctly across dialog/toast/dropdown-menu demo pages.
      Verified dark→light theme toggle re-renders the whole page cleanly (visual
      screenshot). Toast and dropdown-menu spot-checked via snapshot; full keyboard
      contract for every overlay/form component covered by the existing + newly-added
      e2e suite rather than manually walking all ~40 components by hand.
- [x] Bundle/perf re-measurement, repeating the v0.9 Phase 2 method:
  - Library FESM (`dist/volt/fesm2022/voltui-components.mjs`): **422.5 KB raw**
    (v0.9 baseline: 412 KB) — net-neutral, as expected (Phase 1 removed more code —
    two outputs + a manual RxJS bridge constructor — than the SSR fix added).
  - Docs app, **production build** (`pnpm build` + `wrangler pages dev
dist/analog/public`, matching the v0.9 method exactly — an initial trace against
    the unoptimized dev server showed CLS 1.00, which was purely a dev-server/no-
    prerender artifact and not real; discarded in favor of the production measurement):
    home **LCP 102 ms / CLS 0.00**, components grid **LCP 106 ms / CLS 0.00**, resizable
    demo (the component actually touched this phase) **LCP 143 ms / CLS 0.00** — all
    within or better than the v0.9 baseline (113–143 ms LCP, 0.00 CLS on all pages), no
    regression, and CLS 0.00 on the resizable page specifically confirms the
    `afterNextRender` SSR fix causes no hydration-mismatch layout shift.
- [x] Swept "pre-v1"/"before v1"/"release candidate" language and stale 0.9.0/268-test
      stats across `README.md`, `COMPONENT_STATUS.md`, `CONTRIBUTING.md`, `SECURITY.md`,
      `CLI.md`, `introduction.page.ts`, `index.page.ts` (components grid),
      `ai-{prompt,skill,mcp}.page.ts`, `header.ts`, homepage stat tiles. Found and fixed
      real content drift along the way, not just wording: the introduction page's
      Component Catalog listed 9 components (autofill, combobox, date-picker,
      file-upload, listbox, navigation-menu, resizable, sidebar, theme) as
      "experimental", but `COMPONENT_STATUS.md` has had zero experimental components
      for a while — all nine are actually beta. Collapsed the three-column stable/beta/
      experimental layout to two columns on both pages the discrepancy appeared; kept
      the `experimental` label's code plumbing (badge component, `stabilityLabel`/
      `stabilityClass`) as a still-valid future value, just unused today. `SDD.md` and
      `specs/SPEC.md` intentionally left for Phase 3 (they're that phase's own task, not
      stray language to sweep here).

## Phase 3 — Policy & docs versioning ✅ done (2026-08-06)

- [x] `SDD.md`: bumped header to 1.0.0/Stable, replaced §12 "Roadmap to v1" with a
      permanent "Stability Policy" section (§12.0: breaking→major, features→minor,
      fixes→patch), pointed the historical roadmap detail at `CHANGELOG.md`'s `[0.5.0]`
      through `[1.0.0]` entries instead of duplicating it. Also fixed §11's stale
      component count/list (missing `range-slider`, "40 groups" → 41) and removed the
      "still alpha" line while in the area.
- [x] `specs/SPEC.md`: marked the release ladder (§4) complete with a status column;
      added §7 "Post-1.0" — ordinary semver replaces the per-minor-plan cadence for
      routine work, with the `@volt-ui/core` headless-primitives layer named as the one
      already-reserved exception, not scoped here.
- [x] Added `/docs/versioning` ("Versioning & Stability") docs page: the semver
      promise (major/minor/patch table), what `stable`/`beta`/`experimental` mean now
      (experimental reserved but unused), and the Angular-major support policy carried
      forward from README. Registered in the Getting Started sidebar. Verified live via
      `playwright` MCP (screenshot) — renders correctly, correct nav highlight, links to
      migration-notes and the component catalog resolve.
      Also fixed `migration-notes.page.ts`'s opening paragraph, found by the same "no
      pre-1.0 present-tense language" grep — it still said "Volt UI is pre-1.0" in the
      present tense; now points at the new Versioning page and links `MIGRATION.md`
      (GitHub) for the 0.x → 1.0 guide specifically.

## Phase 4 — Publish

- [x] `CHANGELOG.md` `## [1.0.0]` entry — highlights (removed aliases table, the
      resizable-handle SSR fix, new ARIA-contract test coverage, the versioning page and
      policy docs) + link to `MIGRATION.md`.
- [x] Bumped root `package.json`, `projects/volt/package.json`, `cli/package.json` to
      `1.0.0`. `cli/mcp/package.json` (`volt-ui-mcp`) intentionally left alone — it's its
      own independent versioning line (`0.2.0`), outside the 1.0 ladder per `AGENTS.md`.
- [x] Final `pnpm release:check` green at `1.0.0`: 266/266 tests, lint, typecheck,
      `check:ai-docs`, all three packages (`@voltui/components`, `@voltui/cli`,
      `volt-ui-mcp`) pack clean.
- [x] Added `.wrangler/` to `.gitignore` (found untracked after this session's own
      production-build Lighthouse pass — a real pre-existing gap, not previously
      ignored).
- [ ] Tag `v1.0.0`; publish `publish:lib` + `publish:cli`; deploy docs
      (`pnpm deploy:cf:main`). **Not done in this session** — work is committed on local
      branch `release/v1.0.0`, not pushed. Publishing happens automatically via
      `release.yml` the moment this merges to `main` (confirmed: this is exactly what
      published 0.9.0 today). Pushing the branch, opening a PR, and merging all need
      explicit user go-ahead first — that's a real npm publish + GitHub Release, not
      reversible the way local commits are.
- [ ] Post-publish smoke: `npm i @voltui/components@1.0.0` + `npx @voltui/cli add button`
      in a scratch app; verify install + render. **Deferred until after the real publish
      above** — can't smoke-test a version that isn't on npm yet.
- [ ] GitHub release with notes; update repo description/badges. **Deferred** — same
      reason, `release.yml`'s `scripts/release.mjs` creates the GitHub Release
      automatically on publish; repo description/badge updates are a manual follow-up.

## Phase 5 — User QA pass, live-site sweep ✅ done (2026-08-06)

The user tested the live docs site directly (screenshots of the running `pnpm dev`
instance) and flagged three concerns: visible bugs on the site, too many components
shown as `beta` for a v1 release, and a component preview that appeared misaligned.
Investigated each rather than taking the screenshots at face value:

- [x] **Real bug, fixed**: `src/app/lib/component-metadata.ts` (drives the live
      sidebar + components grid) had drifted out of sync with `COMPONENT_STATUS.md`.
      Seven components — `form-field`, `checkbox`, `switch`, `radio`, `slider`,
      `toggle`, `toggle-group` — showed as `beta` on the site while
      `COMPONENT_STATUS.md` has listed them `stable` for a while. Verified each has
      full `ControlValueAccessor` + substantial spec coverage before relabeling; this
      was the real explanation for "too many beta components" — not that the library
      needs more hardening, the site just wasn't reflecting already-completed work.
- [x] **Real bug, fixed**: 4 demo pages (Tabs' Account/Password panel, Login, Sign Up,
      Settings layouts) had `type="password"` inputs outside a `<form>`, triggering a
      Chrome DOM advisory. Wrapped each in `<form>` with submission prevented.
- [x] **Could not reproduce**: the "preview positioned to the right instead of
      centered" issue from the user's screenshot. Tested `search`, `autofill`,
      `combobox`, and `table` demo pages via fresh navigation, client-side
      route-transition navigation, and at a wide (1710px) viewport — all rendered
      correctly centered in the shared `code-panel.ts` preview container
      (`flex items-center justify-center`). Best explanation: this session restarted
      `pnpm dev` mid-conversation (once for the SSR fix verification, once after);
      the user's already-open browser tab's Vite HMR client likely reconnected to a
      fresh dev-server instance mid-render, a known class of stale-HMR-state glitch.
      Told the user to hard-refresh and report back if it recurs on a clean load.
- [x] **No bug found**: hero section on the homepage — rendered correctly on fresh
      load, stats match current counts (42 components, 266 tests).
- [x] Full site sweep: every component demo page (~40), every layout demo page (10),
      and every getting-started/AI-tool page navigated directly, checking console
      errors/warnings on each. Only the 4 password-field pages above had anything.
- [x] Re-ran `pnpm vitest --run` (266/266), `pnpm check:ai-docs`, and final
      `pnpm release:check` — all green after these fixes.

## Out of scope

- Any new component or feature. The `@volt-ui/core` headless primitives layer
  (post-v1 project with its own future spec).

## Verification commands

```bash
pnpm test:all
pnpm release:check
```
